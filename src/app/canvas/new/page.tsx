export default function NewCanvasPage() {
  return (
    <main className="mx-auto flex w-full max-w-xl flex-1 flex-col gap-6 px-8 py-16">
      <h1 className="text-2xl font-semibold tracking-tight">Start a new canvas</h1>
      <p className="text-zinc-600 dark:text-zinc-400">
        North star capture (original_intent, write-once) — wired in the session-lifecycle story.
      </p>
    </main>
  )
}
